WIP

```lua
require("motion").setup({
    -- key = { { "Mod4" }, "y" },
    key = { {}, "Super_L" },
	exit_key = { "Escape" },
	back_key = "BackSpace",
	default_keys = true,
	mod_hold_continue = true,
	mod_release_close = "after",
	hints_delay = 0,
	timeout = 0,
	browser = "firefox",
	terminal = "kitty",
	stop_on_unknown_key = true,
	auto_select_the_only_choice = true,
})
```
