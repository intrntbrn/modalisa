local M = {}

M.awesome = require("modalisa.presets.awesome")
M.sys = require("modalisa.presets.sys")
M.file = require("modalisa.presets.file")
M.client = require("modalisa.presets.client")
M.tag = require("modalisa.presets.tag")
M.helper = require("modalisa.presets.helper")
M.spawn = require("modalisa.presets.spawn")
M.layout = require("modalisa.presets.layout")
M.metatable = require("modalisa.presets.metatable")
M.modalisa = require("modalisa.presets.modalisa")

return M
